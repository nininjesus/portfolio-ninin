---
import '../../styles/components/separator.css';

interface Props {
  variant?: 'default' | 'active';
}

const { variant = 'default' } = Astro.props;
---
<hr class:list={['separator', `separator-${variant}`]} aria-hidden="true" />
