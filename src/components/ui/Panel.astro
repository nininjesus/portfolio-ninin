---
import '../../styles/components/panel.css';

interface Props {
  sectionId: string;
  ariaLabel?: string;
}

const { sectionId, ariaLabel } = Astro.props;
---
<section id={sectionId} aria-label={ariaLabel} class="panel-section">
  <slot />
</section>
